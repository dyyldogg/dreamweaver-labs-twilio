# Dreamweaver Labs

Marketing site for Dreamweaver Labs, a lead capture platform for lawyers and law firms.

## Scripts

- `npm run dev` starts the local Vite dev server
- `npm run build` creates the production build
- `npm run lint` runs ESLint
